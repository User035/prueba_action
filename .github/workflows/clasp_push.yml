name: Clasp_push
on: [push]
jobs:
  build:
    runs:
    using: 'docker'
    image: 'Dockerfile'
     args:
        - ${{ secrets.ACCESSTOKEN }}
        - ${{ secrets.IDTOKEN }}
        - ${{ secrets.REFRESHTOKEN }}
        - ${{ secrets.CLIENTID }}
        - ${{ secrets.CLIENTSECRET }}
        - ${{ secrets.SCRIPTID }}
        - ${{ secrets.ROOTDIR }}
        - 'push'
        - 'Sample description'
        - ${{ secrets.DEPLOYID }}

